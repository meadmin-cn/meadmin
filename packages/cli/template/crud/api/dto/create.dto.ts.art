import { {{replaceNames.Name}} } from '{{paths.entityPath}}';
import { OmitDtoType } from '@/helper/dto.js';
import { InferAttributes } from '@sequelize/core';

export class {{replaceNames.CreateDto}} extends OmitDtoType(
  {{replaceNames.Name}} as new () => InferAttributes<{{replaceNames.Name}}>, //只保留声明属性
  ['{{entity.pk.join("','")}}'] //排除自动创建的主键
) {
  
}
