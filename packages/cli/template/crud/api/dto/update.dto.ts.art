import { {{replaceNames.Name}} } from '{{paths.entityPath}}';
import { InferAttributes } from '@sequelize/core';

export class {{replaceNames.UpdateDto}} extends ({{replaceNames.Name}} as new () => InferAttributes<{{replaceNames.Name}}>) {
  
}
