import { {{replaceNames.Name}} } from '{{paths.entityPath}}';
import { InferAttributesLoose } from '@/../types/entity.js';

export class {{replaceNames.UpdateDto}} extends ({{replaceNames.Name}} as new () => InferAttributesLoose<{{replaceNames.Name}}>) {
  
}
