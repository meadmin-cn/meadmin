{{each properties}}{{if !entity?.pk?.includes($index)}}{{$index}} = <% include('./valueDefault.ts.art', {...$value, key:$index ,entity:entity, needAs:true}) %>;  //{{$value.description}}
{{/if}}{{/each}}
